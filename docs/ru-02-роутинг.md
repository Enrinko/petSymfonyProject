# 2. Подключение страниц к роутам

## Как работает роутинг в Symfony

Symfony сопоставляет HTTP-запрос (URL + метод) с методом контроллера. Роуты в этом проекте объявляются с помощью **PHP-атрибутов** прямо над методами контроллеров — это современный и предпочтительный подход.

## Цепочка: запрос → роут → контроллер → ответ

```
Браузер GET /healthz
    → Symfony Router читает атрибут #[Route('/healthz')]
    → Вызывается HealthController::live()
    → Возвращается JsonResponse / Response
    → Браузер получает ответ
```

---

## Текущие роуты проекта (основные)

Пример простейшего контроллера — `src/Controller/HomeController.php`:

```php
namespace App\Controller;

use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;

final class HomeController extends AbstractController
{
    #[Route('/', name: 'app_home')]
    public function home(): Response
    {
        return $this->render('home/index.html.twig');
    }
}
```

| URL | Метод | Контроллер | Возвращает |
|-----|-------|-----------|-----------|
| `/` | GET | `HomeController::home()` | HTML (дашборд) |
| `/login`, `/register`, … | GET | `SecurityController` | HTML |
| `/api/login`, `/api/register` | POST | `SecurityController` / `RegistrationController` | JSON |
| `/api/clients…` | GET/POST/PUT/DELETE/PATCH | `Api\ClientController` | JSON (CRM-ядро) |
| `/api/admin/users…` | GET/PATCH | `Api\Admin\UserController` | JSON (ROLE_ADMIN) |
| `/healthz`, `/readyz` | GET | `HealthController` | JSON (пробы) |

Полный список: `docker compose exec php bin/console debug:router`.

---

## Как добавить новый роут (пошагово)

### 1. Создать метод в контроллере

Добавьте новый метод в существующий или новый контроллер. Минимальный пример:

```php
// src/Controller/HomeController.php

#[Route('/about')]
public function about(): Response
{
    return $this->render('about.html.twig');
}
```

### 2. Ограничить HTTP-метод (опционально)

```php
#[Route('/api/user', methods: ['POST'])]
public function createUser(Request $request): JsonResponse
{
    $data = json_decode($request->getContent(), true);
    // обработка...
    return new JsonResponse(['status' => 'created'], 201);
}
```

### 3. Параметры в URL

```php
// URL вида /user/42
#[Route('/user/{id}', methods: ['GET'])]
public function showUser(int $id): Response
{
    // $id автоматически инжектится из URL
    return $this->render('user/show.html.twig', ['id' => $id]);
}
```

### 4. Именованный роут

```php
#[Route('/about', name: 'app_about')]
public function about(): Response
{
    return $this->render('about.html.twig');
}
```

Имя роута используется при генерации URL в шаблонах:

```twig
<a href="{{ path('app_about') }}">О нас</a>
```

---

## Как создать новый контроллер

Через консольную команду (внутри контейнера):

```bash
docker compose exec php bin/console make:controller ProductController
```

Symfony автоматически создаст:
- `src/Controller/ProductController.php` — с базовым роутом
- `templates/product/index.html.twig` — пустой шаблон

---

## Как посмотреть все зарегистрированные роуты

```bash
docker compose exec php bin/console debug:router
```

Пример вывода:

```
 Name                 Method   Path
 app_home             ANY      /
 api_clients_list     GET      /api/clients
 app_healthz          GET      /healthz
 _preview_error       ANY      /_error/{code}.{_format}
```

Получить подробности конкретного роута:

```bash
docker compose exec php bin/console debug:router app_home
```

---

## Конфигурация роутинга

### config/routes.yaml

```yaml
controllers:
  resource: routing.controllers
```

Symfony сканирует все классы в `src/Controller/` и собирает роуты из PHP-атрибутов `#[Route(...)]`.

### config/packages/routing.yaml

```yaml
framework:
  router:
    utf8: true
  default_uri: '%env(DEFAULT_URI)%'
```

`default_uri` используется для генерации абсолютных URL в CLI-контексте (например, в командах консоли).

---

## Генерация URL в контроллерах

```php
// Через имя роута (предпочтительно)
$url = $this->generateUrl('app_about');

// Абсолютный URL
use Symfony\Component\Routing\Generator\UrlGeneratorInterface;
$url = $this->generateUrl('app_about', [], UrlGeneratorInterface::ABSOLUTE_URL);
```

---

## Передача параметров в шаблон

```php
#[Route('/products')]
public function list(): Response
{
    $products = ['Apple', 'Banana', 'Cherry'];

    return $this->render('product/list.html.twig', [
        'products' => $products,
    ]);
}
```

В шаблоне `templates/product/list.html.twig`:

```twig
{% for product in products %}
    <li>{{ product }}</li>
{% endfor %}
```

---

## Роуты для API (JSON-ответы)

```php
#[Route('/api/products', methods: ['POST'])]
public function createProduct(Request $request): JsonResponse
{
    $payload = json_decode($request->getContent(), true);

    if (!\is_array($payload)) {
        return ApiJson::invalidJson();
    }

    // валидация команды, вызов Application-хендлера…

    return $this->json(['id' => 1, 'name' => $payload['name'] ?? ''], Response::HTTP_CREATED);
}
```

`$this->json()` — короткий способ вернуть `JsonResponse` с правильными заголовками.

### Единый формат ошибок API

Все ошибки под `^/api` отдаются конвертом `{"message": string, "errors": object|null}`:

- в контроллерах — через хелпер `App\Controller\Api\ApiJson` (`error()`, `validationError()`, `invalidJson()`);
- «неожиданные» исключения (404 по несуществующему пути, 403, 429, 500) приводит к тому же виду `App\Infrastructure\Http\ApiExceptionListener`;
- `errors` заполняется только у ошибок валидации (карта «поле → сообщение»), в остальных случаях `null`.

Полное описание — схема `Error` в `docs/api/openapi.yaml`.